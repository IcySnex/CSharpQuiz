<UserControl
    x:Class="CSharpQuiz.Views.Questions.ResultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CSharpQuiz.Views.Questions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,0,-12">
        <ui:Card Padding="12">
            <TextBlock TextWrapping="Wrap">
                <Run Text="Herzlichen Glückwunsch! Du hast es geschafft, das Quiz ist zuende." />
                <LineBreak />
                <Run Text="Deine Antworten sind unten gekennzeichnet, sowie die Lösungen in grün" />
                <LineBreak />
                <Run Text="Außerdem siehst du in der unteren Leiste Infos wie die Punktezahl oder die Zeit, die du gebraucht hast." />
            </TextBlock>
        </ui:Card>


        <ItemsControl
            Margin="12"
            ItemTemplateSelector="{StaticResource QuestionResultTemplateSelector}"
            ItemsSource="{Binding Questions}" />
    </StackPanel>
</UserControl>
